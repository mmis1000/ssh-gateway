# Ssh Gateway
a proxy to create reverse tunnel, port forward, authication and domain in one command

# Usage

    curl http://<your domain>/setup > run.sh
    chmod 755 run.sh
    ./run.sh

And then... *Magics*  
It create everything required to enable port forwarding and authication for you